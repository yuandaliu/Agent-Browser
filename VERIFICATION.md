# ✅ 手动验证方案（验收清单）

本文件是「浏览器本地小模型智能体」的**人工验收清单**。项目已通过自动化验证的部分会标注
「⚙️ 已自动验证」，其余请按下述步骤在浏览器中手动执行并勾选。

---

## 0. 前置准备

```bash
# 终端 1：模型下载代理（务必先启动）
npm run proxy
# 终端 2：开发服务器
npm run dev
```

打开浏览器访问 **http://127.0.0.1:5189**

| 前置项 | 检查内容 | 通过 |
| --- | --- | --- |
| 0.1 | 页面顶部显示「本地智能体」，左上角徽章为 `WebGPU ✓` | ☐ |
| 0.2 | 按下 `F12` 打开控制台，**无红色报错** | ☐ |
| 0.3 | 左侧模型下拉有 5 个 1B 级模型选项，默认选中 Qwen3.5 0.8B | ☐ |

> ⚙️ 0.1–0.3 已由 `tests/e2e-probe.mjs` 自动验证通过（WebGPU adapter + device 均可用，控制台零错误）。

---

## Phase 1 — 环境与基础骨架

| # | 步骤 | 预期结果 | 通过 |
| --- | --- | --- | --- |
| 1.1 | 观察页面加载按钮 | 左侧「加载模型」主色按钮清晰可见 | ☐ |
| 1.2 | 点击聊天输入框 | 输入框处于禁用状态（灰色），提示先加载模型 | ☐ |
| 1.3 | 控制台 | 无任何 JS 报错（`[model-loader]` 等日志属正常） | ☐ |

> ⚙️ 1.1–1.3 已自动验证通过。

---

## Phase 2 — 模型加载引擎（进度条 0 → 100%）

| # | 步骤 | 预期结果 | 通过 |
| --- | --- | --- | --- |
| 2.1 | 点击「加载模型」 | 按钮变为「加载中…」并禁用；出现进度条，百分比从 0% 开始增长 | ☐ |
| 2.2 | 观察进度条过程 | 进度条从 0% 平滑走到 100%，状态文本显示下载/初始化阶段（如 `Fetching wasm`、文件下载） | ☐ |
| 2.3 | 等待加载完成 | 进度 100%；出现绿色「模型已就绪，可以开始对话」提示条；按钮变为「已就绪 ✓」；顶栏模型徽章变绿 | ☐ |
| 2.4 | 控制台 | 输出 `✅ 模型已就绪: Qwen3.5-0.8B-q4f16_1-MLC` | ☐ |
| 2.5 | 刷新页面后再次点击加载 | 权重已缓存（IndexedDB），重新加载明显更快（秒级到几十秒） | ☐ |

> ⚙️ 2.1–2.4 已由 `tests/e2e.mjs --only-load` 自动验证通过（真实下载 447MB 权重 + WebGPU 加载，进度 100%、控制台就绪、零报错）。
> 首次加载约需下载 447MB（视网速 3–15 分钟），第二次起走缓存。

---

## Phase 3 — ReAct 核心循环（思考-行动-观察）

| # | 步骤 | 预期结果 | 通过 |
| --- | --- | --- | --- |
| 3.1 | 输入 `现在几点` 并回车/点发送 | 助手消息上方出现可折叠的「思考过程」块，含 `Thought / Action` 与 `🔧 Action get_current_time` | ☐ |
| 3.2 | 等待回复完成 | 回复包含**当前时间**（年份、日期、几点几分，与系统时间一致） | ☐ |
| 3.3 | 折叠/展开思考过程 | 点击思考过程标题可收起/展开（`▾/▸`） | ☐ |

**判定标准**：智能体通过「思考→调用 get_current_time 工具→观察→给出最终答复」完成回答，且回复中的时间与系统当前时间一致。

| # | 步骤 | 预期结果 | 通过 |
| --- | --- | --- | --- |
| 3.4 | 输入 `12*34 等于多少` | 思考过程出现 `🔧 Action calculate {"expression":"12*34"}` 与 `👁 Observation 12*34 = 408` | ☐ |
| 3.5 | 等待回复 | 回复答案为 **408** | ☐ |

---

## Phase 4 — 工具系统与记忆

### 4a. 两位数四则运算

| # | 输入 | 预期答案 | 通过 |
| --- | --- | --- | --- |
| 4.1 | `56+27 等于多少` | 83 | ☐ |
| 4.2 | `99-31 等于多少` | 68 | ☐ |
| 4.3 | `12*34 等于多少` | 408 | ☐ |
| 4.4 | `84/12 等于多少` | 7 | ☐ |
| 4.5 | `(12+34)*2 等于多少` | 92（验证括号优先级） | ☐ |

> ⚙️ 计算器核心（`safeEvaluate`）已由单元测试覆盖：两位数加减乘除、优先级、括号、小数、负数、拒绝非法输入（注入、除零、超长）全部通过。

### 4b. 跨轮对话记忆（记住上一轮对话）

| # | 步骤 | 预期结果 | 通过 |
| --- | --- | --- | --- |
| 4.6 | 输入 `我叫小明` | 智能体正常回应（记住或确认） | ☐ |
| 4.7 | 下一轮输入 `我叫什么名字` | 智能体回答 **小明**（依据上一轮对话历史，IndexedDB 持久化） | ☐ |
| 4.8 | 点击「🔄 新对话」后再输入 `我叫什么名字` | 对话历史已清空，智能体无法再从**对话历史**作答（长期记忆不在此验证） | ☐ |

### 4c. 长期记忆（save_memory / recall_memory 工具）

| # | 步骤 | 预期结果 | 通过 |
| --- | --- | --- | --- |
| 4.9 | 输入 `记住我的城市是北京` | 思考过程出现 `🔧 Action save_memory`，观察为「已记住：city = 北京」 | ☐ |
| 4.10 | 点击「🔄 新对话」清空历史 | 对话历史清空 | ☐ |
| 4.11 | 输入 `我的城市是哪里` | 智能体调用 `recall_memory` 并回答 **北京**（长期记忆跨会话保留，IndexedDB） | ☐ |

> 说明：1B 小模型对工具调用的格式遵循存在不确定性，若 4.9 中模型未调用 `save_memory`（思考过程无该工具），请改问 `请记住我的城市是北京` 或重试；4.11 若模型未主动调用工具，可提问 `回忆一下我的城市`。

### 4d. 读取当前页面内容（read_page_content）

| # | 步骤 | 预期结果 | 通过 |
| --- | --- | --- | --- |
| 4.12 | 在任意页面（如 `search.html`）打开聊天弹窗，输入 `总结一下这个页面讲了什么` | 思考过程出现 `🔧 Action read_page_content`，观察返回页面标题与正文 | ☐ |
| 4.13 | 等待回复 | 智能体基于观察到的页面内容给出总结（如"这是一个仿百度的本地智能搜索页面…"） | ☐ |
| 4.14 | 修改页面内容后再提问（如弹窗内输入 `现在页面上有什么新内容`） | 读取到的是**实时最新内容**（MutationObserver 维护缓存） | ☐ |
| 4.15 | 在弹窗内提问 `页面网址是什么` | 智能体可从页面信息（system 提示注入）或 `read_page_content` 回答 URL | ☐ |

> ⚙️ 4.12–4.14 的底层能力已自动验证：全页快照（标题+正文）、selector 指定区域读取、
> selector 未命中/内容过少自动回退整页、智能体自身 UI 从快照排除、DOM 变化后实时刷新
> （E2E 17/17 通过）。模型侧的工具调用需真实加载模型后手动验证。

---

## Phase 5 — UI 优化与流式输出

| # | 步骤 | 预期结果 | 通过 |
| --- | --- | --- | --- |
| 5.1 | 发送任意问题 | 回复以**打字机流式**逐字渲染，末尾有闪烁光标 `▌` | ☐ |
| 5.2 | 观察整体界面 | 深色渐变主题、圆角卡片、消息气泡左右分列（用户右/助手左）、头像区分 | ☐ |
| 5.3 | 快速连发两条消息 | 发送期间发送按钮禁用，防止重复提交；完成后恢复 | ☐ |
| 5.4 | 多行输入 | `Shift+Enter` 换行、`Enter` 发送；输入框自动增高 | ☐ |
| 5.5 | 新对话 | 聊天区清空并显示「新对话已开始」，模型保持已加载状态（无需重新下载） | ☐ |
| 5.6 | 窄窗口（<900px） | 布局切换为单列，仍可正常使用 | ☐ |

---

## 部署验证（Vercel / Netlify，任选其一）

| # | 步骤 | 预期结果 | 通过 |
| --- | --- | --- | --- |
| 6.1 | `npm run build` | 产出 `dist/`（已配置好） | ☐ |
| 6.2 | 推送到 GitHub，在 Vercel 导入 | 自动识别 `vercel.json`（构建命令 + `/hf*`、`/gh-raw*` rewrites → hf-mirror.com / jsdelivr） | ☐ |
| 6.3 | 或推送到 Netlify | 自动识别 `netlify.toml`（构建命令 + 200 状态透明代理 redirects） | ☐ |
| 6.4 | 访问线上地址 | 页面正常加载；点击「加载模型」可从线上**同源路由**下载模型（无需本地代理）；加载完成后可对话 | ☐ |
| 6.5 | 线上控制台 | 无 CORS / 混合内容报错 | ☐ |

> 生产页面自动以 `modelSource: "proxy"` + `verifyProxy: false` 运行（平台 rewrites 无法附加
> `X-Proxy-Worker` 头）。模型文件经线上同源 `/hf/*` 路由从 hf-mirror.com 下载。

---

## 已验证汇总（自动化结果）

| 项 | 结果 |
| --- | --- |
| 单元测试（vitest）：ReAct 解析器 / 安全计算器 / 工具 / 记忆 | **44/44 通过** |
| 页面探测：加载按钮、模型下拉、WebGPU、控制台零报错 | ✅ 通过 |
| 真实模型加载：447MB 权重下载 → 进度 0→100% → 「模型已就绪」 | ✅ 通过（headless Chrome + RTX 4060 WebGPU） |
| 生产构建 `npm run build` | ✅ 成功（dist/ 产出完整） |

---

## 常见问题排查

| 现象 | 处理 |
| --- | --- |
| 徽章显示 `WebGPU ✗` | 更新 Chrome/Edge 到最新版，在浏览器设置中开启硬件加速；确认显卡驱动正常 |
| 进度条长时间卡在某个百分比 | hf-mirror 偶发超时，代理已内置 3 次重试；稍等或点「重试加载」 |
| 提示「Proxy Worker is not active」 | 确认 `npm run proxy` 已启动且端口 8787 未被占用 |
| 模型回答与工具调用格式不规范 | 1B 模型能力有限，属预期；可换 2B/4B 模型（在 `src/modelLoader.js` 的 `MODEL_OPTIONS` 中扩展）或重试提问 |
| 想完全离线 | 首次加载完成后权重已在浏览器缓存，断网后刷新页面即可离线使用（同一浏览器/站点） |
